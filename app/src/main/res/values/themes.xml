<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BookBox" parent="Theme.AppCompat.Light">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/appBar</item>
        <item name="titleTextColor">@color/white</item>
        <item name="colorOnBackground">@color/appBar</item>
    </style>


    <style name="Base.Theme.BookBox_Add" parent="Theme.AppCompat.Light">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/appBar</item>
        <item name="titleTextColor">@color/white</item>
        <item name="colorOnBackground">@color/appBar</item>
    </style>
</resources>

